# .readthedocs.yml

version: 2

build:
  image: stable

formats:
  - pdf
  - epub
  - htmlzip


python:
  version: 3.6
  install:
    - method: pip
      path: .
      extra_requirements: 
        - rtd
  system_packages: true

